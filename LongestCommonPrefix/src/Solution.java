

public class Solution {
    public static String getLongestCommonPrefix(String[] inputs) {
        String result = "";

        //step through word
        for (int i = 0; i < inputs.length - 1; i++) {

            //step through chars in word
            for(int j = 0; j < inputs[i].split("").length; j++){

            }
        }
        return result;
    }
}
